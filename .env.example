PORT=8080
DATABASE_URL=postgres://budget:budget@localhost:5432/budget?sslmode=disable
FIREBASE_CREDENTIALS_FILE=./private-key.json
# AUTH_MODE=dev
# INDICATOR_IPC_SERIES_ID defaults to INDEC's national IPC nivel general
# (see internal/indicator/datosgobar.go); only set this to override it.
